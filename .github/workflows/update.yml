# test update mechanism
name: Update

on:
  #push:
  #  branches: 
  #    - main
  #  paths:
  #    - ccache/*
  #    - .github/workflows/ccache.yml
  workflow_dispatch:
  #schedule:
  #  - cron: "0 * * * *"

env:
  DOCKER_CLI_EXPERIMENTAL: enabled
  REPOSITORY: ${{ github.actor }}/${{ github.workflow }} 

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:

  deploy:
    runs-on: ubuntu-20.04

    steps:
      - name: Source checkout
        uses: actions/checkout@v2.4.0 
        with:
          fetch-depth: 1
 
#      - name: Add packages
#        run: |
#          DEBIAN_FRONTEND=noninteractive sudo apt-get install -y --no-install-recommends git curl jq
#          ls -la
#          pwd

      - name: Update dependencies
        run: |
          PAT=${{ secrets.UPDATE_SECRET }} scripts/test.sh
          git push

#      - name: Source checkin
#        run: |
#          git push

#      - name: Push changes back into repo
#        uses: ad-m/github-push-action@master
#        with:
#          github_token: ${{ secrets.GITHUB_TOKEN }}
#          branch: ${{ github.ref }}

